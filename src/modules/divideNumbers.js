import divide from "./divide";

export function divideTen(num) {
  return divide(num,10);
}

export function divideFive(num) {
  return divide(num, 5);
}

export function divideOne(num) {
    return divide(num, 1)
}